- Fecha: 2026-03-09

## Resumen
- Ajuste de historial de mantenimiento para usar fecha_hasta y calcular km recorridos desde esa fecha con legacy kilométrico.
- Se agregó formato europeo para km (puntos) y se actualizó el layout del form de ingreso.
- Se agregó bloque “Última Reparación” con campos Tipo/Fecha/Km/Días y prefill en base al último ciclo aplicable.

## Cambios realizados
- `apps/tickets/domain/services/intervention_suggestion.py`: historial usa `date_until` (fecha_hasta) en el resumen.
- `apps/tickets/infrastructure/services/legacy_kilometrage.py`: nuevo método `get_km_since()` para sumar km desde una fecha.
- `apps/tickets/application/use_cases/maintenance_entry_use_case.py`: historial con km desde fecha_hasta y prefill actualizado.
- `apps/tickets/presentation/templates/tickets/maintenance_entry_form.html`: reemplazo de “Sugerencia” por “Historial”, y layout compacto con “Última Reparación” (Tipo/Fecha/Km/Días).
- `apps/tickets/presentation/forms/maintenance_entry_forms.py`: “Km RG” ahora acepta string con separadores; campos readonly para última intervención.
- `apps/tickets/presentation/views/novedad_views.py`: prefill de Km RG + última intervención del ciclo (incluye A/AB/ABC/N*), km desde esa intervención y días.

## Estado actual
- UI muestra km formateados y nuevos campos de “Última Reparación”.
- “Período (meses)” sigue existiendo en el form pero ya no se muestra en la UI.
- Los kilómetros aún se leen desde archivos legacy (`context/db-legacy/*.txt`).

## Pendiente para mañana
1) Migrar kilometrajes a `app.db` (modelo + migración + import inicial desde TXT/CSV).
2) Ajustar `LegacyKilometrageRepository` para usar DB en lugar de archivos legacy.
3) Revisar que el prefill de “Última Reparación” sea correcto para todos los tipos de UM (según docs).
4) Verificar UI final luego de la migración.

## Notas
- No se ejecutaron tests luego del último ajuste de UI y prefills.
